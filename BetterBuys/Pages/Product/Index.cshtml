@page
@model BetterBuys.Pages.Product.IndexModel

<section class="d-flex justify-content-start align-items-center flex-wrap">
    @foreach (var product in Model.Products)
    {
        <div style="width: 33%;">
            <partial name="_ProductPartial" for="@product" />
        </div>
    }
</section>